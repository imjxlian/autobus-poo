package tec;

import tec.Autobus;

class PassagerAnxieux extends MonteeSportif {
  public PassagerAnxieux(String nom, int destination, Arret arret) {
    super(nom, destination, arret);
  }
}
